@model MovieCatalog.ViewModels.AuthViewModel
@{
    ViewData["Title"] = "Регистрация";
}

<h1>@ViewData["Title"]</h1>
@if (Model.ErrorMessage != null)
{
    <div class="alert alert-danger">
        @Model.ErrorMessage
    </div>
}

@if (Model.SuccessMessage != null)
{
    <div class="alert alert-success">
        @Model.SuccessMessage
    </div>
}
<form method="post" action="@Model.FormPostUrl">
    <input type="text" name="username" placeholder="Имя пользователя" class="form-control" required />
    <input type="password" name="password" placeholder="Пароль" class="form-control" required />
    <input type="password" name="repassword" placeholder="Повтор пароля" class="form-control" required />
    <button type="submit" class="btn btn-primary">Зарегистрироваться</button>
</form>